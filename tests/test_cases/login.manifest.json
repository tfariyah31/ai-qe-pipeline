{
  "feature_name": "login",
  "total_scenarios": 9,
  "scenarios": [
    {
      "title": "Successful login",
      "tag": "@smoke",
      "endpoint": "POST /api/auth/login",
      "spec_source": "The application uses JWT-based authentication with security best practices.",
      "type": "happy_path"
    },
    {
      "title": "Invalid login credentials",
      "tag": "@regression",
      "endpoint": "POST /api/auth/login",
      "spec_source": "Account lockout after 3 failed login attempts (5 minute lock)",
      "type": "negative"
    },
    {
      "title": "Missing login credentials",
      "tag": "@regression",
      "endpoint": "POST /api/auth/login",
      "spec_source": "The application uses JWT-based authentication with security best practices.",
      "type": "edge_case"
    },
    {
      "title": "Account lockout after 3 failed login attempts",
      "tag": "@regression",
      "endpoint": "POST /api/auth/login",
      "spec_source": "Account lockout after 3 failed login attempts (5 minute lock)",
      "type": "edge_case"
    },
    {
      "title": "Successful logout",
      "tag": "@smoke",
      "endpoint": "POST /api/auth/logout",
      "spec_source": "The system enforces role permissions using backend middleware.",
      "type": "happy_path"
    },
    {
      "title": "Logout without being logged in",
      "tag": "@regression",
      "endpoint": "POST /api/auth/logout",
      "spec_source": "The system enforces role permissions using backend middleware.",
      "type": "negative"
    },
    {
      "title": "Refresh token without being logged in",
      "tag": "@regression",
      "endpoint": "POST /api/auth/refresh",
      "spec_source": "The application uses JWT-based authentication with security best practices.",
      "type": "negative"
    },
    {
      "title": "Successful refresh token",
      "tag": "@smoke",
      "endpoint": "POST /api/auth/refresh",
      "spec_source": "The application uses JWT-based authentication with security best practices.",
      "type": "happy_path"
    },
    {
      "title": "Refresh token with invalid refresh token",
      "tag": "@regression",
      "endpoint": "POST /api/auth/refresh",
      "spec_source": "The application uses JWT-based authentication with security best practices.",
      "type": "negative"
    }
  ],
  "confidence": 0.85,
  "flagged": [
    "Ambiguous account lockout behavior"
  ]
}